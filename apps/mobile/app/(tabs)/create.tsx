import { View } from "react-native";
/** Placeholder: the Post tab's press is intercepted in the tabs layout and opens the /create modal. */
export default function CreateTab() {
  return <View />;
}
